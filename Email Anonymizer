# Create a string variable to hold the email address
email = input("Enter your email address: ")

# Split the email into the username and domain
parts = email.split('@')
username = parts[0]
domain = parts[1]

# Get the first and last letters of the username
first_letter = username[0]
last_letter = username[-1]

# Print the formatted output
print(f"{first_letter}{last_letter}***{domain}")
